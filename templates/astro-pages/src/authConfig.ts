type AuthConfig = {
	pocketbaseUrl: string
	pocketbaseGroup: string
}

const authConfig: AuthConfig = {
	pocketbaseUrl: "https://api.levinkeller.de",
	pocketbaseGroup: "example",
}

export default authConfig
